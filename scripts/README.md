# Scripts

| Script | Purpose |
|--------|---------|
| `diagnose_gateway.sh` | Health report: LM Studio, gateway process, launchd, logs |
| `macos_launchd_gateway.sh` | Bootstrap/restart `com.hermes.gateway` LaunchAgent |
| `read_hermes_start_agents.sh` | Read `hermes_start_agents` from `vars.yml` |
| `read_yaml_value.sh` | Parse a key from YAML |
| `validate_vars.sh` | Check required vars before deploy |

Full usage: [docs/gateway.md](../docs/gateway.md)
