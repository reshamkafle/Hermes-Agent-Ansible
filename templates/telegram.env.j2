# LM Studio API key (same as Hermes ~/.hermes/.env when auth is enabled).
OPENAI_API_KEY="{{ hermes_model_api_key | default('lm-studio', true) }}"
LM_API_TOKEN="{{ hermes_model_api_key | default('lm-studio', true) }}"
LMSTUDIO_BASE_URL="{{ lmstudio_base_url }}"
TELEGRAM_BOT_TOKEN="{{ telegram_bot_token }}"
TELEGRAM_ALLOWED_USERS="{{ telegram_allowed_users | join(',') }}"
TELEGRAM_HOME_CHANNEL="{{ telegram_chat_id }}"
TELEGRAM_SEND_USERS="{{ (telegram_send_users | default(telegram_allowed_users)) | join(',') }}"
