# Copy to inventory.ini and replace with your remote host(s).
# Do NOT add localhost or 127.0.0.1 — deploy_all.sh targets remote machines only.

[hermes_agents]
# my-server ansible_host=192.168.1.100 ansible_user=ubuntu

[hermes_agents:vars]
ansible_python_interpreter=/usr/bin/python3
