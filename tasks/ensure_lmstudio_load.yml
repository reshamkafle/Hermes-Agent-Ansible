---
# Load the configured LM Studio model into memory when not already loaded.
# Requires: lmstudio_home, lmstudio_lms_cmd, target_home, target_user, hermes_path_prefix,
#           lmstudio_effective_model, lmstudio_model_base_id, lmstudio_runtime_model,
#           hermes_model_api_key
# Sets: lmstudio_model_loaded, lmstudio_runtime_model (refreshed from disk when needed)

- name: Ensure Hermes log directory exists (for lms load output)
  file:
    path: "{{ target_home }}/.hermes/logs"
    state: directory
    mode: '0755'
  become: "{{ 'yes' if (ansible_facts['os_family'] == 'Debian') else 'no' }}"
  become_user: "{{ target_user if (ansible_facts['os_family'] == 'Debian') else omit }}"

- name: Confirm LM Studio model on disk before load
  include_tasks: verify_lmstudio_model_on_disk.yml

- name: Require LM Studio model on disk before load
  fail:
    msg: >-
      Cannot load {{ lmstudio_model_base_id }} — model is not on disk.
      Re-run ./deploy_local.sh to download automatically.
      Log: {{ target_home }}/.hermes/logs/lms-get.log
  when: not (lmstudio_model_on_disk | default(false) | bool)

- name: Build LM Studio model match needles for loaded-model detection
  set_fact:
    lmstudio_model_slug: "{{ lmstudio_model_base_id.split('/')[-1] | lower }}"
    lmstudio_load_target: "{{ lmstudio_disk_model_key }}"
    lmstudio_match_needles: >-
      {{
        [
          lmstudio_model_base_id | lower,
          (lmstudio_model_base_id.split('/')[-1] | lower),
          (lmstudio_disk_model_key | lower),
          (lmstudio_disk_model_key | lower | regex_replace(':.*$', ''))
        ] | unique | reject('equalto', '') | list
      }}

- name: Show LM Studio model load plan
  debug:
    msg: >-
      lms load target: {{ lmstudio_load_target }} --yes |
      match needles: {{ lmstudio_match_needles | join(', ') }}


- name: List LM Studio models loaded in memory
  command: "{{ lmstudio_lms_cmd }} ps --json"
  environment:
    HOME: "{{ target_home }}"
    PATH: "{{ hermes_path_prefix }}:{{ ansible_facts['env']['PATH'] }}"
    LM_API_TOKEN: "{{ hermes_model_api_key | default('lm-studio', true) }}"
    TERM: dumb
    NO_COLOR: "1"
  register: lms_ps_json
  changed_when: false
  failed_when: false
  become: "{{ 'yes' if (ansible_facts['os_family'] == 'Debian') else 'no' }}"
  become_user: "{{ target_user if (ansible_facts['os_family'] == 'Debian') else omit }}"

- name: Detect configured model in loaded memory listing
  set_fact:
    lmstudio_model_already_loaded: >-
      {%- set models = (lms_ps_json.stdout | from_json | default([])) if (lms_ps_json.rc | default(1) == 0) else [] -%}
      {%- set needles = lmstudio_match_needles | default([]) -%}
      {%- set ns = namespace(found=false) -%}
      {%- for m in models -%}
        {%- for field in ['identifier', 'modelKey', 'path', 'indexedModelIdentifier'] -%}
          {%- set val = (m[field] | default('') | string | lower | regex_replace(':.*$', '')) -%}
          {%- for n in needles -%}
            {%- if n and (n == val or n in val or val in n) -%}
              {%- set ns.found = true -%}
            {%- endif -%}
          {%- endfor -%}
        {%- endfor -%}
      {%- endfor -%}
      {{ ns.found }}

- name: Note configured model already loaded in memory
  debug:
    msg: >-
      LM Studio model already loaded:
      {{ lmstudio_load_target }}
      (skipping lms load)
  when: lmstudio_model_already_loaded | default(false) | bool

- name: Load LM Studio model into memory (macOS)
  shell: >-
    "{{ lmstudio_lms_cmd }}" load
    {{ lmstudio_load_target | quote }}
    --yes
    >> "{{ target_home }}/.hermes/logs/lms-load.log" 2>&1
  environment:
    HOME: "{{ target_home }}"
    PATH: "{{ hermes_path_prefix }}:{{ ansible_facts['env']['PATH'] }}"
    LM_API_TOKEN: "{{ hermes_model_api_key | default('lm-studio', true) }}"
    TERM: dumb
    NO_COLOR: "1"
  async: "{{ lmstudio_load_async_seconds | default(3600) }}"
  poll: "{{ lmstudio_load_poll_interval | default(30) }}"
  register: lms_load_macos
  when:
    - ansible_facts['os_family'] != 'Debian'
    - not (lmstudio_model_already_loaded | default(false) | bool)
  become: false

- name: Load LM Studio model into memory (Linux / WSL2)
  shell: >-
    "{{ lmstudio_lms_cmd }}" load
    {{ lmstudio_load_target | quote }}
    --yes
    >> "{{ target_home }}/.hermes/logs/lms-load.log" 2>&1
  environment:
    HOME: "{{ target_home }}"
    PATH: "{{ hermes_path_prefix }}:{{ ansible_facts['env']['PATH'] }}"
    LM_API_TOKEN: "{{ hermes_model_api_key | default('lm-studio', true) }}"
    TERM: dumb
    NO_COLOR: "1"
  async: "{{ lmstudio_load_async_seconds | default(3600) }}"
  poll: "{{ lmstudio_load_poll_interval | default(30) }}"
  register: lms_load_linux
  when:
    - ansible_facts['os_family'] == 'Debian'
    - not (lmstudio_model_already_loaded | default(false) | bool)
  become: true
  become_user: "{{ target_user }}"

- name: Record LM Studio load command exit status
  set_fact:
    lms_load_command_succeeded: >-
      {{
        (
          lms_load_macos is defined
          and not (lms_load_macos.skipped | default(false) | bool)
          and (lms_load_macos.rc | default(1) == 0)
        )
        or (
          lms_load_linux is defined
          and not (lms_load_linux.skipped | default(false) | bool)
          and (lms_load_linux.rc | default(1) == 0)
        )
      }}
  when: not (lmstudio_model_already_loaded | default(false) | bool)

- name: Re-check LM Studio models loaded in memory after load
  command: "{{ lmstudio_lms_cmd }} ps --json"
  environment:
    HOME: "{{ target_home }}"
    PATH: "{{ hermes_path_prefix }}:{{ ansible_facts['env']['PATH'] }}"
    LM_API_TOKEN: "{{ hermes_model_api_key | default('lm-studio', true) }}"
    TERM: dumb
    NO_COLOR: "1"
  register: lms_ps_after_load
  changed_when: false
  failed_when: false
  when: not (lmstudio_model_already_loaded | default(false) | bool)
  become: "{{ 'yes' if (ansible_facts['os_family'] == 'Debian') else 'no' }}"
  become_user: "{{ target_user if (ansible_facts['os_family'] == 'Debian') else omit }}"

- name: Detect configured model in memory after load attempt
  set_fact:
    lmstudio_model_loaded_after_command: >-
      {%- set models = (lms_ps_after_load.stdout | from_json | default([])) if (lms_ps_after_load is defined and lms_ps_after_load.rc | default(1) == 0) else [] -%}
      {%- set needles = lmstudio_match_needles | default([]) -%}
      {%- set ns = namespace(found=false) -%}
      {%- for m in models -%}
        {%- for field in ['identifier', 'modelKey', 'path', 'indexedModelIdentifier'] -%}
          {%- set val = (m[field] | default('') | string | lower | regex_replace(':.*$', '')) -%}
          {%- for n in needles -%}
            {%- if n and (n == val or n in val or val in n) -%}
              {%- set ns.found = true -%}
            {%- endif -%}
          {%- endfor -%}
        {%- endfor -%}
      {%- endfor -%}
      {{ ns.found }}
  when: not (lmstudio_model_already_loaded | default(false) | bool)

- name: Determine if LM Studio model load completed
  set_fact:
    lmstudio_model_loaded: >-
      {{
        (lmstudio_model_already_loaded | default(false) | bool)
        or (lms_load_command_succeeded | default(false) | bool)
        or (lmstudio_model_loaded_after_command | default(false) | bool)
      }}

- name: Read LM Studio load log tail on failure
  command: tail -n 40 "{{ target_home }}/.hermes/logs/lms-load.log"
  register: lms_load_log_tail
  changed_when: false
  failed_when: false
  when:
    - not (lmstudio_model_loaded | default(false) | bool)
    - not (lmstudio_model_already_loaded | default(false) | bool)

- name: Capture last LM Studio ps JSON on failure
  set_fact:
    lms_ps_after_load_debug: "{{ lms_ps_after_load.stdout | default('(lms ps unavailable)') | trim }}"
  when:
    - not (lmstudio_model_loaded | default(false) | bool)
    - not (lmstudio_model_already_loaded | default(false) | bool)

- name: Require LM Studio model load to succeed
  fail:
    msg: >-
      `lms load {{ lmstudio_load_target }} --yes` failed.
      Load command exit code:
      {{ lms_load_macos.rc | default(lms_load_linux.rc | default('n/a')) }}.
      Full log: {{ target_home }}/.hermes/logs/lms-load.log
      Last `lms ps --json`: {{ lms_ps_after_load_debug | default('(none)') }}
      Re-run: ./deploy_local.sh
      Or: {{ lmstudio_lms_cmd }} load {{ lmstudio_load_target }} --yes
      Then verify with: {{ lmstudio_lms_cmd }} ps --json
      If guardrails block loading, set modelLoadingGuardrails mode to "off" in LM Studio settings.
      Last log lines:
      {{ lms_load_log_tail.stdout | default('(log empty or missing)') }}
  when:
    - not (lmstudio_model_loaded | default(false) | bool)
    - not (lmstudio_model_already_loaded | default(false) | bool)
